# 🎯 SCOPE — ส่วนของโครงงานที่เลือกทำใน Module 2

**โครงงาน:** my-coffee-store2t — สั่งกาแฟผ่านมือถือที่โต๊ะ (Self-Order)
**ส่วนที่เลือก:** ออเดอร์เครื่องดื่มจากโต๊ะ ที่บาริสต้ากดเปลี่ยนสถานะ
**เทียบแนวโครงงานในคู่มือข้อ 3️⃣:** 🛒 ร้านค้า → หยิบเฉพาะใบสั่งซื้อ ไม่ทำทั้งร้าน

> ต้นทางความต้องการ: [`docs/01-requirements/01-spec/self-order.md`](docs/01-requirements/01-spec/self-order.md)
> โครงสร้างข้อมูล: [`docs/02-design/02-technical/data-structure.md`](docs/02-design/02-technical/data-structure.md)
> ของที่เกินขอบเขต: [`docs/01-requirements/03-task/backlog.md`](docs/01-requirements/03-task/backlog.md) หัวข้อ Backlog Sprint 2

---

## 2️⃣ ประโยคเดียว

> ระบบของฉันเก็บ **ออเดอร์เครื่องดื่ม** ที่ **ลูกค้าที่นั่งโต๊ะ** สร้างขึ้น
> แต่ละรายการเลือก **เมนูเครื่องดื่ม** ได้ และมีสถานะ **รับออเดอร์แล้ว → กำลังทำ → เสร็จแล้ว**
> โดย **บาริสต้า** เป็นคนกดเปลี่ยน

## 1️⃣ เช็กครบ 6 อย่าง

| # | LeaveEasy | ร้านกาแฟของเรา | ครบ |
|---|---|---|---|
| 1 | 📁 `leaveRequests` รายการหลัก | 📁 `orders` ออเดอร์ที่เข้ามาทุกวัน | ✅ |
| 2 | ✏️ `requesterId` เป็นของใคร | ✏️ `customerId` ลูกค้าเจ้าของออเดอร์ · `tableNo` โต๊ะที่สั่ง | ✅ |
| 3 | ✏️ `status` สถานะที่เปลี่ยนได้ | ✏️ `status` รับออเดอร์แล้ว → กำลังทำ → เสร็จแล้ว | ✅ |
| 4 | 📁 `leaveTypes` ประเภทให้เลือก | 📁 `menus` เมนูเครื่องดื่ม | ✅ |
| 5 | 📁 `approvals` โฟลเดอร์ย่อย | 📁 `messages` ข้อความระหว่างร้านกับลูกค้าเรื่องออเดอร์นั้น | ✅ |
| 6 | 📝 `reason` ข้อความยาว | 📝 `note` หมายเหตุถึงบาริสต้า เช่น แพ้นมวัว ขอน้ำแข็งน้อย | ✅ |

**ผล: ครบ 6 ข้อ**

## 6️⃣ ตารางขอบเขต (ใช้ทั้ง 4 สัปดาห์)

| | 🔧 LeaveEasy | ☕ ร้านกาแฟของเรา |
|---|---|---|
| 📁 โฟลเดอร์หลัก | `leaveRequests` | `orders` |
| 📁 โฟลเดอร์ประเภท | `leaveTypes` | `menus` |
| 📁 โฟลเดอร์ย่อย | `approvals` | `messages` |
| ✏️ ช่องบอกว่าเป็นของใคร | `requesterId` · `requesterName` | `customerId` · `tableNo` |
| 🔀 สถานะทั้งหมด | รอพิจารณา · อนุมัติ · ไม่อนุมัติ | รับออเดอร์แล้ว · กำลังทำ · เสร็จแล้ว |
| 👤 คนที่สร้างรายการ | พนักงาน | ลูกค้าที่นั่งโต๊ะ |
| 👤 คนที่เปลี่ยนสถานะ | หัวหน้า | บาริสต้า |
| 📝 ช่องข้อความยาวที่ AI จะอ่าน | `reason` | `note` |
| 🤖 งานที่ AI ช่วย (สัปดาห์ที่ 8) | จัดประเภทการลาให้อัตโนมัติ | อ่านหมายเหตุ แล้วติดธงเตือนบาริสต้าเรื่องแพ้อาหารหรือส่วนผสมที่ต้องระวัง |

> 🔒 ลูกค้าไม่ต้องสมัครสมาชิก และระบบไม่เก็บชื่อหรือเบอร์โทรลูกค้าตาม PDPA (BL-08)
> จึงใช้ `customerId` เป็นรหัสผู้ใช้แบบไม่ระบุตัวตน คู่กับ `tableNo` แทนชื่อลูกค้า

## 🔁 เทียบกับที่ทำในคาบ

| ในคาบทำกับ LeaveEasy | ของเรา |
|---|---|
| 📁 `leaveRequests` = โฟลเดอร์หลัก | 📁 `orders` |
| 📁 `users` = คนที่ใช้ระบบ | 📁 `users` พนักงานร้าน: บาริสต้า · เจ้าของร้าน |
| 📁 `leaveTypes` = ประเภทที่เลือกได้ | 📁 `menus` อเมริกาโน่ · ลาเต้ · มัทฉะลาเต้ |
| 📁 `approvals` = โฟลเดอร์ย่อยของใบลา | 📁 `messages` ข้อความเรื่องออเดอร์นั้น |
| สถานะ รอพิจารณา → อนุมัติ / ไม่อนุมัติ | รับออเดอร์แล้ว → กำลังทำ → เสร็จแล้ว |
| `requesterName` จดชื่อซ้ำไว้ | `menuName` · `baristaName` · `authorName` |

## 5️⃣ ขอบเขตที่พอดี

| | จำนวน | ของเรา |
|---|---|---|
| 📁 โฟลเดอร์หลัก | 1 | `orders` |
| 📁 โฟลเดอร์ประกอบ | 2 | `users` · `menus` |
| 📁 โฟลเดอร์ย่อย | 1 | `messages` |
| 🖥️ หน้าจอ | 3–4 | สั่งเครื่องดื่ม · รายการออเดอร์ · รายละเอียดออเดอร์ + ปุ่มเปลี่ยนสถานะ · (จัดการเมนู) |
| 👤 บทบาทผู้ใช้ | 2–3 | ลูกค้า · บาริสต้า · (เจ้าของร้าน) |
| 🔀 สถานะ | 3 | รับออเดอร์แล้ว · กำลังทำ · เสร็จแล้ว |

> 1 ออเดอร์ = เครื่องดื่ม 1 เมนู เพื่อคุมขอบเขต 4 สัปดาห์ · ตะกร้าหลายเมนูในออเดอร์เดียวอยู่ใน Backlog Sprint 2

## 🔐 กติกาการเห็นข้อมูล (ใช้ตอนสัปดาห์ที่ 7–8)

- ลูกค้าเห็นเฉพาะออเดอร์ที่ `customerId` เป็นของตัวเอง
- บาริสต้าและเจ้าของร้านเห็นออเดอร์ทั้งหมด และบาริสต้าเป็นคนเปลี่ยนสถานะ
